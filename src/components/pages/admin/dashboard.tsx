"use client";
import MaxWidthWrapper from "@/components/wrapper";
import { useAppState } from "@/context/global-context";
import { cn } from "@/lib/utils";
import React from "react";

function Dashboard() {
  return (
    <MaxWidthWrapper>Dashboard</MaxWidthWrapper>
  );
}

export default Dashboard;
